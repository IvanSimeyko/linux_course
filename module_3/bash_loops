Ветвления основы.
Синтаксис:
if [[ условие ]]
then
    # действие если условие истинно
elif [[ условие 2 ]]
then
    # действие, если условие 1 ложно,
    # но условие 2 истино
else
    # действие если оба условия ложны
fi

Ветвление: альтернативы
Синтаксис:
case переменная in
знач1)
    #действие, если переменная==знач1
    ;;
знач2)
    #действие, если переменная==знач2
    ;;
*)
    # действие если переменная не равна
    # ни одному из вариантов


Условия(строки):
-z <строка>   # строка пуста
-n <строка>   # строка не пуста
<стр1> == <стр2>   # строки равны
<стр1> != <стр2>   # строки не равны

Условия числа(сторки):
<число/строка>операция<число/строка>
-eq, (==)    # равно (equal)
-ne, (!=)    # не равно
-lt, (<)    # меньше (less than)
-le,    # меньше или равно 
-gt, (>)    # больше (grait than)
-ge,    # больше или равно

Условия (файлы):
-e <путь>    # путь существует
-f <путь>    # это файл
-d <путь>    # это директория
-s <путь>    # размер файла больше 0
-x <путь>    # файл исполняемый

Условия логические:
!    # отрицание логического выражения
&&    # логическое "И"
||    # логическое "ИЛИ"
